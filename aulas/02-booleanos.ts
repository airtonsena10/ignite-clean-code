// Booleanos

const isUserIsOfLegalAge = true

// Nomear como se estivesse fazendo uma pergunta (is, does, has)

const disabled = false
const isDisabled = false

// se está desabilitado
if (isDisabled) {

}

// Booleans sempre como pergunta / semântico

const loading = true
const isLoading = true

// Nomenclatura

const isUserProfileLoading = true

// Evitar nomes genéricos (loading, disabled, list, data, fetching)
// Para saber se o booleano está semântico, //
//ler como se fosse uma conversa e se está seguindo as regras da gramática